server {
	listen  8002;
	host 0.0.0.0;

	server_name	localhost:8002;
	index		index.html, index.htm;

	location / {
		root 			./test42;
		index			index.html;
		methods			GET, POST;
		cgi_extension	.bla;
		cgi_bin			./ubuntu_cgi_tester;
	}
}
